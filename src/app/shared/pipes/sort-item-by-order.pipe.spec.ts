import { SortItemByOrderPipe } from './sort-item-by-order.pipe';

describe('SortItemByOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new SortItemByOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
